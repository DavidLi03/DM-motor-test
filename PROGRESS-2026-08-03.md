# DM 电机调试工作阶段总结（二）

- **日期**：2026-08-02（下午）～ 2026-08-03
- **工作目录**：`/home/faber-alpha/Workspace/DM-motor-test`
- **阶段目标**：电机运动闭环（已达成）＋ 反馈链路瓶颈排除（已达成）
- **被测对象**：DM-J4310-2EC V1.2（48V，驱动板 V4）+ DM-USB2FDCAN 模块
- **前篇**：[PROGRESS-2026-08-02.md](PROGRESS-2026-08-02.md)（本篇对其 §1.1 / §4.5 有勘误）

> **一句话结论：方案 B' 纯阻尼运动闭环达成并经人工目视复核；查明出厂固件按 100ms
> 定时攒批投递反馈（有效反馈率仅 10Hz），经批准刷入 slcan 固件走 SocketCAN 后，
> 反馈延迟降至 0.4~0.6ms、1kHz 无损，瓶颈彻底消除；电机侧物理测量跨路线复现一致。**

---

## 一、设备状态变化（与前篇的「零改动」不同，本阶段有两项实质变更）

| 层面 | 变化 |
|---|---|
| **模块固件** | ✅ **已刷写**：出厂 `app 1.0.0.5` → **slcan `1.0.0.2`**（`dm_usb2fdcan_slcan_1002.enc`），2026-08-03 由用户经上位机执行。**可逆**：重刷 `factory-firmware/dm_usb2canfd_app_1006.enc` 即可换回（厂商文档确认可互刷；回退动作需重新报备） |
| **电机使能/运动** | ✅ **已按约定报备并获批**：`0xFB` 清错、`0xFC` 使能、MIT 纯阻尼旋转（多次，最大行程 31.5°，峰值转速 0.50 rad/s）、`0xFD` 失能。每次运行均以失能收尾 |
| 电机固件 | ❌ 无变化（7118） |
| **电机寄存器** | ❌ **无变化**（全程未发 `0x55` 写参数 / `0xAA` 存参数 / `0xFE` 存零点） |
| 主机 · 软件包 | ✅ 安装 `can-utils`（slcand/candump/cansend） |
| 主机 · 网络接口 | ✅ `slcand -o -c -s5 /dev/ttyACM0 can0` + `txqueuelen 1000`（每次插拔模块后需重建，见 `dmcan-ws/SOCKETCAN.md`） |
| 主机 · 代码 | ✅ `dmcan-ws/` 新增 6 个源文件 + 1 份操作手册（见第六节） |

---

## 二、方案 B'：MIT 纯阻尼低速旋转（SDK 路线，2026-08-02）

控制律：kp=0、tau=0 ⇒ `T = kd·(dq_des − dq)`，纯速度阻尼。
两个与 kd 无关的安全性质：稳态转速天花板只由 dq_des 决定；最大力矩 = kd·dq_des 且只在堵转时出现。
（瞬态可冲高：实测峰值 0.476 rad/s vs 指令 0.281 —— 力矩反号只能减速，拦不住弹性能量释放。）

### 2.1 实验序列与结果

| 实验 | 结果 |
|---|---|
| kd=0.3 固定 | 未转（堵转力矩 0.084 Nm 不足挣脱），但**控制律闭环验证通过**：巡航段实测 tau 0.080 Nm vs 理论 0.0876 Nm，9% 内吻合 |
| `--sweep` kd 0→1.5 | **首次位移 kd≈0.46**（挣脱单个齿槽定位点，0.129 Nm @输出轴）；**持续运动 kd≈0.76**。低 kd 下为粘滑运动：~0.0076 rad 离散跳步夹静止段 |
| kd=1.14 固定 3s | **连续旋转 31.5°**，巡航 0.1727 rad/s = 指令的 61%，粘滑消失（非正速度拍仅 10%） |

### 2.2 摩擦模型：存在 Stribeck 效应

两个工况点反解动摩擦：0.164 Nm @ 0.065 rad/s、0.114 Nm @ 0.180 rad/s
⇒ **摩擦-速度斜率 ≈ −0.43 Nm/(rad/s)**。负斜率即负阻尼，正是低速粘滑的教科书成因。
推论：摩擦不是常数，任何「用单点摩擦值外推转速」的预测都会系统性偏差（我犯过，见 §7）。

### 2.3 纯阻尼的结构性极限

kp=0 且无前馈时，稳态速度误差恒等于 `T_摩擦/kd`，不会消失。
kd 顶到 1.5 上限也只能达到指令速度的 ~61%。要提高达成率需加摩擦前馈或位置环（下一步候选）。

### 2.4 双门限测量规范（写进了 `--sweep`）

- **门限 1（0.002 rad）首次位移** = 挣脱单个齿槽定位点 → 接近真实静摩擦
- **门限 2（0.02 rad）持续运动** = 累积跨过多个定位点 → 高约 65%，**不可当静摩擦用**

---

## 三、出厂固件 100ms 攒批投递的判定（dm_batch，零运动，2026-08-02）

实验设计：只发 `0x7FF+0x33` 读命令（电机失能状态下同样逐条应答），六档速率对比
主机到达时刻与模块硬件时间戳 `hw_ts`。

| 速率 | 每批帧数 | 批间隔（中位） | 应答 |
|---|---|---|---|
| 10 Hz | 1 | 100.16 ms | 20/20 |
| 20 Hz | 2 | 100.12 ms | 40/40 |
| 50 Hz | 5 | 100.13 ms | 100/100 |
| 100 Hz | 10 | 100.19 ms | 200/200 |
| 200 Hz | 20 | 100.16 ms | 400/400 |
| 500 Hz | 50 | 100.25 ms | 1000/1000 |

**判定：纯定时器攒批，非填满即发。** 每批帧数严格线性于速率而批间隔恒定；500Hz 下
每批 50 帧 ≈1KB 已超一个 bulk 包仍不提前投递（缓冲 ≥50 帧）。攒批在模块固件侧
（SDK 是异步 libusb，不会攒）；`dmcan.h` 无接收投递配置项。

**后果：出厂固件路径下应用层有效反馈率只有 10Hz，单帧时效 0~100ms，>10Hz 闭环做不了。**
发送方向不受影响。CAN 总线与电机零缺陷（六档 100% 应答、0 错误、`hw_ts` 间隔=发送周期）。

### 3.1 `hw_ts` 的三个事实

1. **不是平坦 ns 计数器**：高 32 位=秒、低 32 位=秒内纳秒（低 32 位实测最大 998,940,532，
   粒度 4ns）。按平坦 uint64 解读会在每次秒进位产生 `2^32−10^9 = 3.295s` 假跳变。
2. **其时钟比主机慢 3.71%**（中位间隔/标称 = 0.9627~0.9631，跨 50 倍速率高度一致）。
   CAN 1Mbps 零错误说明晶振是准的 ⇒ 时间戳计数器用了另一时钟源（疑似内部 RC，随温漂）。
   **修正系数 1.0385 不可硬编码。** 推论：算速度用「主机发送周期」做时基比 `hw_ts` 准。
3. **批内偶发乱序**：2/1759 = 0.11%（倒退 9.4ms / 25.1ms）。依赖顺序必须按 `hw_ts` 排序。

### 3.2 勘误：前篇 §4.5 的「15.7~21.7 ms 往返延迟」作废

`dm_probe --latency` 的测法结构性错误：拿每帧到达时刻减「最近一次发送」——一批 10 帧
同时到达时全都只和最后一次发送比，**攒批在该测法下不可见**。前篇引为「证伪依据」的
数字本身是错的；被它否定掉的「恰好 100ms」原始观察反而是真信号。
**正确测法：逐帧配对各自的发送时刻，或看到达间隔的分布（双峰一眼可见）。均值必然掩盖。**

---

## 四、固件路线决策（2026-08-02 调研，2026-08-03 用户批准执行）

| 候选 | 结论 |
|---|---|
| **slcan `1002`** ✅ | 厂商实测背书（9 电机@5M 波特率 1kHz 收发可用、500Hz 稳定 ⇒ 逐帧投递坐实）；文档完整；明确可互刷回出厂；本机内核自带 `slcan.ko` |
| gs_usb `1003` ❌ | 仓库内零文档；枚举 VID:PID 未知，本机 6.8 内核 gs_usb 驱动只认领标准 ID（1d50:606f 等）；达妙只给 Jetson/RK3588 的魔改驱动，明言不可跨内核/平台，x86+6.8 不在支持列表 |

**⚠ 桥接波特率的坑：slcand 参数按达妙自定义表，`-s5` = 1Mbps 经典 CAN**
（标准 slcan 表 s8 才是 1M；slcand 只透传 `S5\r`，解释权在固件侧）。
用错档位的症状：接口正常起来但一帧收不到。

---

## 五、slcan 迁移与三级验收（2026-08-03）

### 5.1 通路验证（dm_probe_sc，零运动）

10/10 寄存器读回，值与 SDK 路线实测完全一致；**首帧延迟 0.34~0.59 ms**。

### 5.2 投递时效验收（dm_batch_sc，零运动）

| 速率 | 收/发 | 到达间隔中位 | 配对延迟 中位/p90/最大 (ms) |
|---|---|---|---|
| 10 Hz | 20/20 | 99.95 ms | 0.581 / 0.651 / 0.682 |
| 100 Hz | 200/200 | 10.00 ms | 0.499 / 0.605 / 0.731 |
| 500 Hz | 1000/1000 | 2.00 ms | 0.415 / 0.478 / 0.719 |
| **1000 Hz** | **2000/2000** | **1.00 ms** | **0.414 / 0.461 / 1.352** |

七档全部：批数=帧数（逐帧投递）、100% 应答、0 CAN 错误。
「配对延迟」为逐帧配对发送时刻的真实请求→应答全链路延迟（总线+固件+内核）。
**出厂固件的 10Hz 天花板消除，反馈率上限 ≥1kHz。**

### 5.3 运动闭环（dm_spin_sc，参数与 SDK 路线完全相同）

| 指标 | SDK 路线 (8-02) | SocketCAN (8-03) |
|---|---|---|
| 行程 | +0.5505 rad = 31.5° | +0.5264 rad = 30.2° |
| 巡航速度（位移法） | 0.1727 rad/s（61%） | 0.1683 rad/s（60%） |
| 反解动摩擦 | 0.1232 Nm | 0.1282 Nm |
| 应答率 | 99.0% | **100.0%（410/410）** |
| 非正速度拍占比 | 10% | 6% |

- 跨路线 4% 以内复现 ⇒ 电机侧物理测量与固件路线无关，摩擦模型继续有效。
- 逐帧日志（内核时间戳）：到达间隔中位 10.21ms、最大 10.51ms，零攒批；
  **逐拍速度首次可直接用时间戳计算**（逐拍均值 0.1680 与位移法 0.1683 交叉验证吻合）。
- **2026-08-03 用户手动复跑并人工目视复核：起步顿挫、匀速蠕动、软停、失能均符合预期。**

---

## 六、新增代码（`dmcan-ws/`）

| 文件 | 说明 |
|---|---|
| `src/dm_spin.cpp` | 方案 B'（SDK 路线）：状态机（宽限→升速→匀速/扫描→观察→降速→制动）、双门限 sweep、三重保护、逐帧 CSV |
| `src/dm_batch.cpp` | 攒批机制判定（零运动，只发 0x33） |
| `src/dm_sc.hpp` | SocketCAN 公共层：CAN_RAW + `SO_TIMESTAMPNS` + DM 协议编解码，不链厂商库 |
| `src/dm_probe_sc.cpp` | 通路验证（零运动） |
| `src/dm_batch_sc.cpp` | 投递时效验收：逐帧配对测真延迟（零运动） |
| `src/dm_spin_sc.cpp` | 方案 B' 移植版，状态机/钳位与 dm_spin 逐行一致，加 `--if`、发送失败兜底 |
| `SOCKETCAN.md` | 刷写→桥接→验证→回退 操作手册 |

安全设计（两版 spin 相同）：kp/tau 硬编码 0；dq≤0.5 rad/s、kd≤1.5、单次≤3s 钳位；
超速 1.0 rad/s / 超程 1.5 rad / ERR 异常三重保护；斜坡起停；正常/保护/Ctrl-C 三路径都失能；
20s 总时长兜底。

---

## 七、教训存档（本阶段方法论错误，均已修正）

1. **链路延迟错了两次**：先把真实的 100ms 观察自我否定为代码假象；再用结构性错误的
   测法得出 16~22ms 并写进文档（前篇 §4.5，已勘误）。→ 测延迟必须逐帧配对发送时刻。
2. **`hw_ts` 平坦解码**制造 3.295s 假跳变，一度误判为「模块卡顿」。→ 见 §3.1。
3. **「静摩擦 0.2133 Nm」高估 65%**：0.02 rad 门限需累积 3~4 个齿槽才触发，偏差是
   自己设计的门限引入的。→ 双门限规范（§2.4）。
4. **速度预测失败**（预测 0.137、实测 0.173 rad/s）：假设了摩擦为常数，实际有 Stribeck
   负斜率。→ §2.2。
5. **验收程序自身的判据 bug**：批边界阈值加 1ms 下限，在 1000Hz 档（周期即 1ms）把一半
   正常间隔误判为批内，产生假「仍在攒批」。→ 阈值必须用半周期，无下限。
6. 前篇 §1.1「Linux 无法切换固件」不成立——上位机刷写可用，已实际完成 slcan 切换。

---

## 八、尚未完成 / 下一步候选

| 项 | 状态 |
|---|---|
| 提高速度达成率 | 未做。纯阻尼稳态误差 = T_摩擦/kd（结构性），候选：摩擦前馈、位置环（kp>0 需重新评估安全钳位） |
| 高频闭环（>100Hz） | 链路已具备（1kHz 验收通过），控制程序仍为 100Hz |
| DM 协议层封装成库 | 未做。目前分散在 `dm_sc.hpp`（SocketCAN）与各 SDK 版源文件中 |
| Stribeck 摩擦曲线系统测绘 | 只有两个工况点，斜率 −0.43 为两点线性估计 |
| 出厂固件回退演练 | 未做。SDK 路线程序（dm_probe/dm_hold/dm_spin/dm_batch）在 slcan 固件下不可用，属预期 |

---

## 九、安全约束（长期有效，与前篇相同）

> **电机未固定，直接摆放在桌面上。必须以低转速驱动。
> 启动前必须先告知用户，获得批准后才可使能电机并启动。**

本阶段批准记录：方案 B' 运动测试（用户人工握持外壳+急停待命）、`--sweep` 扫描、
kd=1.14 运行、slcan 固件刷写、SocketCAN 路线 dm_spin_sc 运行（含一次 Claude 直接执行、
一次用户手动复核）。**始终未批准/未发送：`0x55` 写参数、`0xAA` 存参数、`0xFE` 存零点、
电机固件刷写。** 模块固件回退（刷回出厂）需重新报备。
