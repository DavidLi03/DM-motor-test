# DM-J4310-2EC 摩擦力系统辨识（2026-08-03）

程序：`dmcan-ws/src/dm_fric_sc.cpp`（构建目标 `dm_fric_sc`）
分析：`dmcan-ws/tools/fit_friction.py`（纯标准库 Python，无依赖）

## 目标模型

Stribeck + 粘性四参数模型（分方向辨识，检验不对称性）：

```
T_f(ω) = [ Tc + (Ts−Tc)·exp(−(ω/ωs)²) ]·sign(ω) + b·ω
```

| 参数 | 含义 |
|---|---|
| Ts | 静摩擦峰 (Nm) |
| Tc | 库仑摩擦 (Nm) |
| ωs | Stribeck 特征速度 (rad/s) |
| b  | 粘性系数 (Nm·s/rad) |

另产出：转动惯量 J（coast-down 与平台点锚定反推）。

## 两种独立方法互证

1. **恒速平台法**（主）：MIT 纯阻尼（kp=0, tau=0），稳态 kd·(dq_des−ω) = T_f(ω)。
   每平台双法：反解 `kd·(dq_cmd − ω_位移法)`（主值）vs 巡航段 tau 遥测均值（互证，
   偏差 > 15% 剔除）。位移法转速 = 解卷绕行程差/时长。
2. **coast-down 法**：末平台后 kd 归零（保持使能、零力矩、100Hz 持续发帧喂
   ~120ms 看门狗），自由滑停。J·dω/dt = −T_f(ω)；曲线形状独立验证平台法，
   与平台 T_f 锚定得 J。

## 批次与运行命令（`dmcan-ws/build/` 下，用户手动启动）

```bash
./dm_fric_sc --set L --csv fricL1.csv    # 0.08~0.42 rad/s ×5 双向  kd=1.5   ~50s
./dm_fric_sc --set M --csv fricM1.csv    # 0.6~2.5  rad/s ×4 双向  kd=1.5   ~38s
./dm_fric_sc --set H --csv fricH1.csv    # 4.0~9.0  rad/s ×4 双向  kd=4/ω   ~49s（末尾 coast-down）
```

- 每速度点先 +ω 平台再 −ω 平台（换向经斜坡限速器，无速度阶跃），行程自然对消。
- 每平台：斜坡 → 稳定 0.7s → 测量窗（低速 4s / 中高速 3s），逐平台即时打印结果。
- 建议每批跑 2 遍（L 批 3 遍），改 CSV 文件名即可；`--set coast` 可单独重复惯量测试。
- `--speeds 0.5,0.9` 自定义速度点；`--list` 只打印段表不发帧（dry check）。
- 安全框架与 dm_demo_sc 同源：kd·dq ≤ 4.0 Nm 堵转天花板；超速/力矩/温度(70℃)/
  ERR/反馈丢失/兜底六类保护；Ctrl-C ×1 柔停、×2 立即失能；
  只发 0xFB/0xFC/0xFD/MIT，绝不发 0x55/0xAA/0xFE。

## CSV 格式

```
t_s,kts_ns,pos_rad,travel_rad,vel_rad_s,tau_Nm,dq_cmd,kd_cmd,t_mos,t_rotor,seg,phase
```

`t_mos`/`t_rotor` 为反馈帧 byte6/7 的 MOS 与线圈温度（℃，为后续温升补偿留数据；
dm_spin_sc / dm_demo_sc 的 CSV 也已同步加上这两列）。
`seg` = 段号；`phase`: 0=斜坡/稳定 1=测量窗 2=coast。

## 拟合

```bash
python3 ../tools/fit_friction.py fricL1.csv fricL2.csv fricM1.csv fricH1.csv ...
```

- 平台点判定：|ω| < 0.02 rad/s 记「未挣脱」→ 只给 Ts 下界（取 kd·(dq−ω)），不进曲线；
  双法偏差 > `--gate`(0.15) 剔除；其余进拟合。
- 拟合：ωs 对数网格（0.02~2.0，80 点）× 3 参数线性最小二乘（正规方程），
  约束 Ts−Tc ≥ 0、b ≥ 0、Tc > 0；分正/反/合并三组报告参数与 R²。
- coast 段：9 帧滑动窗二次拟合 travel(t) → ω、dω/dt（|ω| > 0.3 rad/s 有效），
  与平台点最近邻锚定 → J 中位数。
- 已用合成数据（已知真值 Tc=0.09/Ts=0.165/ωs=0.15/b=0.014/J=0.005）端到端验证：
  复原 Tc=0.0898/Ts=0.1638/ωs=0.154/b=0.0140/J=0.00493。

## 既有先验（辨识前的 4 个零散工况点，输出轴）

| ω (rad/s) | T_f (Nm) | 来源 |
|---|---|---|
| 0.065 | 0.164 | kd 扫描（dm_spin_sc --sweep） |
| 0.17~0.18 | 0.114~0.128 | 方案 B' 低速 |
| 2.76~2.79 | 0.121~0.140 | demo1 |
| 8.47~8.49 | 0.205~0.212 | demo2 |

粗拟线性段：T_f ≈ 0.092 + 0.0138·ω（>1 rad/s 区间）。本次辨识应给出与之相容、
且低速段完整的曲线。

## 结果（2026-08-03，fricL1/M1/H1.csv，电机 33~34℃）

数据质量：三批帧间隔中位 10.22~10.23ms，唯一 >15ms 间隔为使能前 123ms 静默尾（已知良性）；
26 个平台中 22 个入拟合、3 个未挣脱（给静摩擦下界）、1 个双法偏差 41% 剔除
（−0.134 rad/s，粘滑过渡区）。入拟合点双法互证偏差 2~6%。

### 四参数模型（输出轴，34℃ 时值）

| 数据集 | 点数 | Tc (Nm) | Ts (Nm) | ωs (rad/s) | b (Nm·s/rad) | R² |
|---|---|---|---|---|---|---|
| 正转 | 11 | 0.0998 | 0.1314 | 0.245 | 0.0097 | 0.986 |
| 反转 | 11 | 0.0945 | 0.1244 | 0.260 | 0.0098 | 0.989 |
| **合并** | 22 | **0.0972** | 0.1287 | **0.245** | **0.0097** | 0.977 |

- **真实静摩擦峰高于拟合 Ts**：+0.110 rad/s 平台施加 0.165 Nm 持续 4s 完全未挣脱
  ⇒ **Ts(breakaway) ≥ 0.165 Nm**（拟合的 Ts=0.129 只是运动数据向 ω→0 的外推）。
  与前日 kd 扫描挣脱值 0.129~0.164 一致。挣脱阈随转子停点（齿槽相位）波动：
  −0.085 施加 0.128 未挣脱，−0.134 施加 ~0.10-0.14 却挣脱了。
- **方向不对称 ~5%**（正转略高），远小于 demo1 单次观察到的 15% ⇒ 该 15% 为单次波动。
- 摩擦前馈参考式（>0.3 rad/s 区间，±0.005 Nm 内）：
  `T_ff ≈ [0.097 + 0.032·exp(−(ω/0.245)²)]·sign(ω) + 0.0097·ω`

### 转动惯量（coast-down，−8.6 rad/s 自由滑停 ~4s）

**J ≈ 0.0047 kg·m²（输出轴侧）**，14 个锚点范围 0.0043~0.0050；
折算转子侧 J/Gr² ≈ 4.7×10⁻⁵ kg·m²。coast 独立复算的 T_f 曲线与平台模型在
0.4~6.8 rad/s 吻合在 8% 内（coast 侧斜率略陡：0.0127 vs 0.0097 Nm·s/rad）。

### ⚠ 温度效应已经显现（温升补偿的直接动机）

本次 34℃ 下 8.6 rad/s 摩擦 0.176~0.179 Nm，而 demo2（更早、电机更凉时）同速为
0.205~0.212 Nm —— **同速摩擦低了 ~14%**；粘性系数也从早前两点粗估的 0.0138 降到
0.0097。符合润滑脂粘度随温度下降的规律。结论：**上述参数表是 34℃ 时值**，
做前馈须配温度修正（CSV 已录 t_mos/t_rotor，数据基础已备）。

- [x] 平台点表（L/M/H 各 1 遍有效——第二遍因同名覆盖未留存）
- [x] 四参数模型（正/反/合并）+ R²
- [x] 方向不对称性结论（~5%，可忽略）
- [x] J 估计（coast-down）
- [x] 温度记录基线（33~34℃ 恒定；跨会话对比已见 −14%/+6℃ 量级的摩擦温度效应）

### 力矩刻度的溯源（2026-08-03 寄存器实测补充）

反馈帧的 tau 无力矩传感器，是电流推算：tau = Kt·iq。驱动器的换算链
（手册 V1.2 §扭矩系数）Kt = 1.5·Npp·ψf·GR·GREF，本机寄存器实测：
**ψf(0x13)=0.0046 Wb（出厂逐台标定）、GREF(0x1E)=1.0、⇒ Kt = 0.965 Nm/A @输出轴**。

- 本辨识的所有力矩量纲结果（Tc/Ts/b/T_f/J）共享尺度因子 k = Kt_true/Kt_nom，
  是"驱动器 Nm"单位下的辨识；速度轴与曲线形状（ωs、方向对称性）与 Kt 无关。
- ψf 与 VMAX 公式自洽（0.57735·48/(14·0.0046·10)≈43 vs VMAX=50），排除 Kt 大幅错误；
  出厂反电动势标定的典型精度为百分之几。
- **GREF=1.0 ⇒ 驱动器按无损减速箱换算力矩**，tau 反馈是电磁力矩折算值，
  带载时的真实输出轴力矩还要再扣齿轮损耗与 T_f。
- Inertia 寄存器(0x0C)=2.03e-5 kg·m²（转子侧，折算输出轴 0.00203）vs coast-down
  实测 0.0047，差 2.3×——寄存器疑似裸转子标称值（不含齿轮反射惯量），
  不能当绝对基准用；存疑待查。
- 绝对刻度标定路径（未做）：已知质量×杠杆臂重力标定，双向慢转对消摩擦后拟合
  m·g·r·sin(θ) 项得 k。需要小夹具。
