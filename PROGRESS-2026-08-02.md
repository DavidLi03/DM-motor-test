# DM 电机调试工作阶段总结

- **日期**：2026-08-02
- **工作目录**：`/home/faber-alpha/Workspace/DM-motor-test`（973 MB）
- **阶段目标**：打通 CAN 链路（已达成）
- **被测对象**：DM-J4310-2EC V1.2（48V，驱动板 V4）+ DM-USB2FDCAN 模块
- **主机**：x86_64，Linux 6.8.1-1056-realtime

> **一句话结论：设备侧零改动（固件未刷、寄存器未写、电机未使能），改动全在主机侧。**

> **📌 后续进展见 [PROGRESS-2026-08-03.md](PROGRESS-2026-08-03.md)（运动闭环、攒包判定、slcan 迁移）。
> 该篇对本文两处记述有勘误：§1.1「无法切换固件」、§4.5「15.7~21.7ms 往返延迟」。**

---

## 一、固件版本

### 1.1 USB-to-CANFD 模块

| 项目 | 值 |
|---|---|
| 型号 / 标识 | `DM-USB2FDCAN`（DaMiao-Tech），VID:PID `34b7:6877` |
| 序列号 | `B79F842638FF84CC899D5DE57AE30005` |
| **固件版本** | **`app v1.0.0.5`（即 1005）** |
| 读取方式 | `dmcan_device_print_version()`（新版 DM Device SDK） |
| 固件类型 | **出厂固件**（非 slcan、非 gs_usb） |
| 仓库内可用版本 | `1006`（当前出厂版）；`1001`–`1005` 位于 `usb2canfd/firmware/factory-firmware/legacy-firmware/` |
| **是否刷写** | **否，保持原样** |

早前用 `stty` + `printf 'V\r'` 探测 `/dev/ttyACM0` 无回显，已排除 slcan 固件的可能；
随后 `dmcan_device_print_version()` 直接坐实为出厂固件 1005。

模块固件的三条互斥路线（出厂固件 / slcan / gs_usb 免驱），切换需要达妙上位机升级。

> **勘误（2026-08-03）**：「无法切换」不成立。2026-08-03 已由用户经上位机刷入
> slcan 固件 `1002`，见 [PROGRESS-2026-08-03.md](PROGRESS-2026-08-03.md) 第一、四、五节。

### 1.2 电机 DM-J4310-2EC V1.2（48V，驱动板 V4）

| 寄存器 | 原始字节 | 解读 |
|---|---|---|
| `0x0E` sw_ver | `37 31 31 38` | **ASCII `"7118"` → 版本 7.1.1.8** |
| `0x24` sub_ver | `39 00 00 00` | 57（0x39） |
| `0x25` Boot_ver | `03 02 00 06` | **Boot 3.2.0.6** |

按 `motor-firmware/版本说明/固件版本说明.md` 的命名规则
（4 位数字：前两位 = 系列编码，后两位 = 版本号）：

- `71` = V4 硬件 4310 **48V** 系列码
- `18` = 版本号 V18
- 精确对应仓库文件 **`motor-firmware/固件/V4/APP_DM4310(V4-48V)_V7118_04.bin`**

说明：

1. `sub_ver = 57` 与文件名 `_04` 子版本后缀的对应关系文档未写明，**存疑，未采信**。
2. Boot 3.2.0.6 ≥ 3.2.0.5，按手册**支持通过 CAN 做固件升级**（当前不需要）。
3. 仓库里另有 `APP_DM4310(V4_48V)_V7138_01.bin`（系列 71 / 版本 38），版本号高于当前。
   **未刷写，当前也不建议刷。**

**是否刷写：否，保持原样。**

---

## 二、与开始工作之前的差异

| 层面 | 变化 |
|---|---|
| **模块固件** | ❌ **无变化**（1005 → 1005） |
| **电机固件** | ❌ **无变化**（7118 → 7118） |
| **电机寄存器** | ❌ **无变化**（全程只发 `0x33` 读参数，未发 `0x55` 写参数 / `0xAA` 存参数） |
| **电机使能状态** | ❌ **无变化**（全程未发 `0xFC`，电机从未使能、从未转动） |
| **电机零点 / 控制模式** | ❌ **无变化**（未发 `0xFE` / 未改写 `0x0A`） |
| 主机 · 软件包 | ✅ 安装了 `build-essential` `cmake` `pkg-config` |
| 主机 · udev | ✅ 新增 `/etc/udev/rules.d/99-damiao-usb2canfd.rules`，模块节点为 `crw-rw---- root plugdev`，**免 sudo 可访问** |
| 主机 · 仓库 | ✅ 新增 17 个 git 仓库 |
| 主机 · 代码 | ✅ 新增 `dmcan-ws/`（可用）、`u2canfd-ws/`（已废弃） |
| 认知 | ✅ 见第四节 |

udev 规则内容：

```
SUBSYSTEM=="usb", ATTR{idVendor}=="34b7", ATTR{idProduct}=="6877", MODE="0660", GROUP="plugdev", TAG+="uaccess"
```

---

## 三、已完成的工作

### 3.1 资料准备（17 个仓库，全部来自 `github.com/dmBots`）

| 类别 | 仓库 |
|---|---|
| 关节电机 ×14 | `DM-J3507-2EC` `DM-J4310-2EC` `DM-J4310P-2EC` `DM-J4340-2EC` `DM-J4340P-2EC` `DM-J6006-2EC` `DM-J6248P-2EC` `DM-J8006-2EC` `DM-J8009-2EC` `DM-J8009P-2EC` `DM-J8520P-2EC` `DM-J10010-2EC` `DM-J10010L-2EC` `DM-J10422P-2EC` |
| 工具 ×2 | `usb2canfd`（模块 SDK + 固件）、`motor-firmware`（电机固件 + 版本说明） |
| SDK ×1 | `dm-device-sdk`（新版，v1.1.0） |

> **gitee 在本机 TLS 不通**（`SSL_ERROR_SYSCALL`），官方 gitee 入口用不了，一律走 GitHub。

### 3.2 手册研读

`DM-J4310-2EC/manual/DM-J4310-2EC V1.2减速电机说明书V1.2定稿.pdf`（45 页），
用 `pdftotext -layout` 全文提取并精读，重点是寄存器表、CAN 协议帧格式、波特率规则。

### 3.3 链路打通

- 排查并**放弃**旧版 SDK（`usb2canfd/SDK/legacy/`）
- 建立 `dmcan-ws/` 工作区，链接新版 `libdm_device.so`
- 编写只读探测工具 `dmcan-ws/src/dm_probe.cpp`（支持 `--baud` / `--id` / `--scan`）
- **实测结果：12 条寄存器查询，12 条应答，0 错误**

### 3.4 电机实测配置

| 寄存器 | 读回值 | 与用户告知的信息 |
|---|---|---|
| `0x08` ESC_ID（接收 ID） | `0x08` | ✅ 一致 |
| `0x07` MST_ID（反馈 ID） | `0x18` | ✅ 一致 |
| `0x23` can_br | `4` → **1 Mbps** | ✅ 一致 |
| `0x0A` CTRL_MODE | **`1` = MIT** | ⚠️ 用户未提及，见 4.4 |
| `0x15` PMAX | `12.5` | 与 SDK `limit_param[DM4310_48V]` 一致 |
| `0x16` VMAX | `50` | 同上 |
| `0x17` TMAX | `10` | 同上 |
| `0x10` NPP（极对数） | `14` | — |
| `0x14` Gr（减速比） | `10` | — |

链路实测为**经典 CAN 2.0B**（收发帧均标 `2.0`，`canfd=0`）。

---

## 四、关键结论（踩过的坑）

### 4.1 旧版 SDK 与模块固件不兼容 —— 方向性问题

`usb2canfd/SDK/UPDATE.md` 写明：*「单路 USB2CANFD：`1004` 版本起请谨慎核对兼容性」*。
本机模块是 **1005**。实测旧版 SDK 的 `CMD_SETUP_BUARD` 被固件以 `NACK_ARG_ERR(0x03)` 拒绝，
**所有波特率组合均失败，包括厂家自己示例用的 1M/5M**。

→ `u2canfd-ws/` 这条路（含 `dm_main`、boost 依赖）是死的，已废弃，仅作协议参考。
正确路线是 **DM Device SDK**。

### 4.2 电机工作在经典 CAN 2.0B，不是 CAN FD

手册第 16 页：*「若波特率小于等于 1Mbps，自动变成 CAN 2.0B」*。
电机在 1 Mbps ⇒ **必须用 `canfd=false, brs=false` 发帧**。
厂家示例默认的 1M/5M CANFD 配置在这台设备上永远不可能通。

波特率码表：`0`=125K `1`=200K `2`=250K `3`=500K `4`=1M `5`=2M `6`=2.5M `7`=3.2M `8`=4M `9`=5M

### 4.3 ⚠️ 旧版 SDK 有安全陷阱

`Motor_Control` **构造函数就会调用 `enable_all()`**，其中会：

1. `switchControlMode(VEL)` —— **改写电机 CTRL_MODE 寄存器**
2. 发 `0xFC` —— **使能电机**

也就是说「只 new 一个对象、先不动电机」在旧版 SDK 里**做不到**。
此外 `main.cpp:153` 还写死了 `control_vel(motor, 3.0)`。
对一台摆在桌上没固定的电机，直接跑示例程序是危险的。

→ 探测工具绕开了整个 `Motor_Control`，直接走底层裸 CAN 帧，只发文档化的 `0x33` 读参数。

### 4.4 ⚠️ 电机当前是 MIT 模式，不是速度模式

`CTRL_MODE = 1 (MIT)`。若按速度模式写控制代码会与电机实际状态不符
（或被迫改写 `0x0A` 寄存器）。已确认后续按 MIT 模式推进。

### 4.5 ⚠️ 通讯丢失看门狗 —— 连续三次调试失败的根因

**使能状态下，CAN 总线静默过久会触发 `ERR=0xD 通讯丢失`，电机自动退出使能。**

根因是**自己的代码**：`burst()` 发完命令后固定 `sleep(120ms)` 等应答，
使能成功后立刻制造了 120 ms 总线静默，被电机判为超时。
前三次失败（200Hz 版、100Hz 版）全部是同一原因，与电机、与接线均无关。

去掉这段静默后一次通过：5 秒、480 帧 MIT、应答率 100%、ERR 全程为 1、零跳变。
因果关系由此直接验证。

三条必须遵守的推论：

1. **发完 `0xFC` 后必须立即开始周期性发控制帧**，不能留出明显静默。
2. **不能「先等 ack 确认使能、再开始发控制帧」** —— 等 ack 期间总线是静默的，
   本身就会触发看门狗。正确做法是**边发边校验**：发完 `0xFC` 立即进入控制循环，
   宽限期（现为 150ms）之后再回读 `ERR` 确认。
3. **该故障位会锁存。** 电机长时间空闲后故障位一直挂着，`0xFC` 并不清除它，
   必须先发 `0xFB` 清错，回读确认 `ERR=0` 后才能使能。

> **阈值未精确测定。** 已知边界：静默 **120 ms 必触发**，静默 **~15 ms 不触发**，
> 真实阈值在两者之间。寄存器 `0x09 TIMEOUT` 读回 **400**，手册未标注单位
> （若为 0.25 ms 则恰好 100 ms，但这纯属猜测，无证据支持）。
> 工程上按「控制帧周期 ≤ 10 ms」处理即可，不依赖精确值。

#### 一次被证伪的假设（记录以免重犯）

> **⚠️ 勘误（2026-08-02 深夜，dm_batch 判定）：本小节下面的「证伪」本身是错的，
> 不要采信。** `dm_probe --latency` 的测法结构性失效：它拿每帧到达时刻减「最近一次
> 发送」，一批 10 帧同时到达时全都只和最后一次发送比，攒批在该测法下不可见——
> **15.7~21.7ms 这个数字作废**。真相：出厂固件按 100ms 纯定时器攒批投递反馈帧，
> 「恰好 100ms」的原始观察是真信号。完整判定与正确测法见
> [PROGRESS-2026-08-03.md](PROGRESS-2026-08-03.md) 第三节。以下保留原文存档：

曾据 `[ERR跳变]` 时间戳推断「收帧延迟约 100 ms」，理由是两次跳变间隔总是恰好 100 ms。
**该结论是错的。**（勘误：其实是对的——见上）`dm_probe --latency` 实测（25 ms 间隔连发 20 次）：

- 主机侧往返延迟 **15.7 ~ 21.7 ms**（勘误：测法无效，作废）
- 模块硬件时间戳 `hw_ts` 相邻间隔 24.4~24.6 M（单位纳秒，即 24.5 ms），与发送节拍吻合
 （此条属实——`hw_ts` 实为 秒:纳秒 双 32 位结构且时钟慢 3.71%，详见后篇 §3.1）

那个「恰好 100 ms」是 `burst()` 里写死的 tail sleep 制造出来的节拍，
被误当成了链路特性。（勘误：tail sleep 只是巧合地也是 100ms，链路确有 100ms 攒批。）
**教训：不能只用收帧时刻反推链路延迟，必须逐帧配对各自的发送时刻，并看间隔分布而非均值。**

其余保护阈值实测（均正常，不是本次故障来源）：
低压 `0x00`=15V、过压 `0x1D`=60V、过温 `0x02`=100℃、过流 `0x03`=0.8。

---

## 五、DM CAN 协议速查（已从旧版 SDK 源码逐条核实）

### 5.1 命令帧 —— 发往 `0x7FF`，8 字节

| 用途 | 数据 |
|---|---|
| 读参数 | `ID_L, ID_H, 0x33, RID, 0,0,0,0` |
| 写参数 | `ID_L, ID_H, 0x55, RID, D0,D1,D2,D3` |
| 存 flash | `ID_L, ID_H, 0xAA, 0x01, ...` |
| 刷新状态（未文档化） | `ID_L, ID_H, 0xCC, 0x00, ...` |

应答帧格式与请求同构：`D0=ID_L D1=ID_H D2=0x33 D3=RID D4..D7=数据(小端)`。
`RID` 为 7–10、13–16、35–36 时按 `uint32` 解释，其余按 `float`。

### 5.2 控制命令帧 —— 发往 `CAN_ID + 模式偏移`，8 字节 `FF FF FF FF FF FF FF <cmd>`

| cmd | 含义 |
|---|---|
| `0xFC` | 使能 |
| `0xFD` | 失能 |
| `0xFE` | 保存零点 |
| `0xFB` | 清除错误 |

模式偏移：MIT `0x000`、位置速度 `0x100`、速度 `0x200`、力位混控 `0x300`。
本机 CTRL_MODE=1(MIT)，故控制帧一律发往 **`0x008`**。

### 5.3 MIT 控制帧（发往 `0x008`，8 字节）

```
D0 = q_uint[15:8]
D1 = q_uint[7:0]
D2 = dq_uint[11:4]
D3 = (dq_uint[3:0] << 4) | kp_uint[11:8]
D4 = kp_uint[7:0]
D5 = kd_uint[11:4]
D6 = (kd_uint[3:0] << 4) | tau_uint[11:8]
D7 = tau_uint[7:0]
```

编码 `float_to_uint(x, xmin, xmax, bits)`：
`data_uint = ((x - xmin) / (xmax - xmin)) * ((1 << bits) - 1)`

| 量 | 范围 | 位宽 |
|---|---|---|
| q（位置） | ±PMAX = ±12.5 | 16 |
| dq（速度） | ±VMAX = ±50 | 12 |
| kp | 0 ~ 500 | 12 |
| kd | 0 ~ 5 | 12 |
| tau（前馈力矩） | ±TMAX = ±10 | 12 |

### 5.4 反馈帧（来自 `MST_ID = 0x018`，8 字节）

```
D0 = ID | (ERR << 4)
D1 = POS[15:8]
D2 = POS[7:0]
D3 = VEL[11:4]
D4 = (VEL[3:0] << 4) | T[11:8]
D5 = T[7:0]
D6 = T_MOS      (MOS 温度, ℃)
D7 = T_Rotor    (线圈温度, ℃)
```

解码 `uint_to_float(x, xmin, xmax, bits)`：
`data = (x / ((1 << bits) - 1)) * (xmax - xmin) + xmin`

ERR 码：`0`=失能 `1`=使能 `8`=超压 `9`=欠压 `A`=过电流
`B`=MOS 过温 `C`=线圈过温 `D`=通讯丢失 `E`=过载

---

## 六、尚未完成

| 项 | 状态 |
|---|---|
| 电机控制协议层 | 部分完成。新版 SDK **只提供裸 CAN 收发**，DM 协议已按第五节在 `dmcan-ws/src/dm_hold.cpp` 中实现（清错/使能/失能/MIT 帧编码/反馈解码），尚未封装成库 |
| 使能 | **已验证**：`0xFB` 清错 → `0xFC` 使能 → 回读 `ERR=1`，应答率 100%，CAN 错误 0 |
| 零力矩保持（方案 A） | **已完成并验证**：5 秒 / 480 帧 MIT / 应答率 100% / CAN 错误 0 / `ERR` 全程为 1 且零跳变 / `pos` 恒为 -1.5421 rad 未动 / 温度 34-33℃ 无变化 |
| 转动（方案 B） | 未做。需用户批准 |
| 反馈帧 1 kHz 闭环 | 未做。当前 100 Hz |

已修复：`dm_probe` 对 `0x0E`(sw_ver) 和 `0x25`(Boot_ver) 的显示格式
（原先把 ASCII / 字节序版本号当成 `uint` / `float` 打印为 `942747959`、`2.40756e-35`）。

---

## 七、安全约束（长期有效）

> **电机未固定，直接摆放在桌面上。必须以低转速驱动，避免安全事故。
> 启动前必须先告知用户，获得批准后才可使能电机并启动。**

配套要求：任何会让电机转动的程序都必须带失能（`0xFD`）退出路径，
包括正常退出、超时退出和 Ctrl-C 中断。
