# 达妙电机调试工作总结（2026-08-02）

> **📌 时点快照，撰写于 slcan 迁移之前。** 最新进度（slcan 刷写、SocketCAN 验收、
> 运动闭环复核）见 [PROGRESS-2026-08-03.md](PROGRESS-2026-08-03.md)。

工作目录：`~/Workspace/DM-motor-test`，硬件为 **USB2CANFD 模块（VID:PID 34b7:6877，原厂固件 app v1.0.0.5）** + **DM-J4310-2EC V1.2 (48V)，驱动板 V4**（`sw_ver` "7118"，Boot 3.2.0.6）。电机**未固定，裸放桌面**，全程按「低速 + 运行前须用户批准」的安全约束执行。

## 一、技术路线决策（已定，不再回头）

| 决策 | 理由 |
|---|---|
| 用 **C++ SDK**，不用 Python | Python SDK 只带 cp38/cp310/cp313 的预编译 `.so`，本机只有 Python 3.12 |
| 用**新版 dm-device-sdk**，不用 `usb2canfd/SDK/legacy/` | 新版为官方现行维护路径 |
| 帧格式：**经典 CAN 2.0B**（`canfd=false, brs=false`） | 手册规定波特率 ≤1Mbps 自动为 2.0B；发 CANFD 帧电机收不到 |

## 二、写了哪些测试例程

工程在 `dmcan-ws/`，CMake 构建四个目标，均链接 `lib/libdm_device.so`（RPATH 已设好，无需 LD_LIBRARY_PATH）：

### 1. `dm_probe` — 总线探测 / 寄存器读取
- 通过 `0x7FF` + `0x33` 读参数命令，读回电机全部关键寄存器（见第四节）。
- 其 `--latency` 模式后来被证明**测法结构性错误**（见第五节教训 2），结果作废。

### 2. `dm_hold` — 方案 A，MIT 零增益「握手」验证
- 使能 → 周期发 kp=0/kd=0/tau=0 的 MIT 帧 → 校验反馈与 ERR → 失能。验证了使能/失能流程、看门狗规避方法、反馈帧解码。

### 3. `dm_spin` — 方案 B'，MIT 纯阻尼低速旋转（本次核心例程，~660 行）
- 控制律：kp=0、tau=0，仅 kd×(dq_des−dq)，即**纯速度阻尼**。两条与 kd 无关的安全性质：转速天花板只由 dq_des 决定；最大力矩 = kd×dq_des 且只在堵转时出现。
- 状态机：宽限 → 升速(0.3s) → 匀速 → 观察 → 降速(0.3s) → 制动(0.3s)，另有 20s 总时长硬保险。
- 安全钳位：`dq ≤ 0.5 rad/s`、`kd ≤ 1.5`、单次 ≤3s、超速 1.0 rad/s / 越位 1.5 rad 立即中止。
- `--sweep` 模式：kd 从 0 线性爬升 6s，用**双门限**报告——门限 1（0.002 rad，挣脱单个齿槽定位点）与门限 2（0.02 rad，持续运动），并注明用门限 2 当「静摩擦」会高估。
- `--csv` 逐帧日志：`t_s, hw_ts_ns, pos_rad, vel_rad_s, tau_Nm, dq_cmd, kd_cmd`。
- 自查修掉一个**关键 bug**：降速→制动切换时复用了循环顶部的旧 `ts`，会把制动段整段跳过，电机带速失去主动阻尼。修法：判断处用 `el - st_t0` 重算。

### 4. `dm_batch` — 零运动 USB 攒包测量例程
- **设计上物理不可能让电机动**：全文唯一发送调用只发 `0x7FF + 0x33 读`（RID=0x0A，只读），无 0xFC/0xFD/0xFB/0xFE/0x55/0xAA/MIT 帧，电机全程失能。
- 六档发送速率（10/20/50/100/200/500Hz），绝对时基调度（`sleep_until`），每档前置 300ms 静默 + 250ms 收尾冲刷。
- 记录每帧的主机到达时刻、模块硬件时间戳 `hw_ts`、CAN ID、方向，输出 CSV。

## 三、USB2CANFD 模块固件的发现（本次最大成果）

1. **反馈帧按 100ms 定时器批量投递，且是纯定时器、非填满即发。** 六档速率下每批帧数严格线性（1/2/5/10/20/50），批间隔恒为 100.08~100.47ms；500Hz 下每批 50 帧 ≈1KB 已超一个 512 字节 bulk 包仍不提前投递（缓冲 ≥50 帧）。攒包发生在**模块固件侧**（SDK 是异步 libusb，不会攒）；`dmcan.h` 无任何接收投递配置项。
   - **工程后果：经此 SDK 路径，应用层有效反馈率只有 10Hz，单帧时效 0~100ms。>10Hz 的闭环控制在此路径上做不了。发送方向不受影响。**
2. **CAN 总线与电机完美无缺，瓶颈 100% 在模块 USB 投递侧。** 六档应答率全部 100%（含 500Hz 的 1000/1000），0 CAN 错误，`hw_ts` 帧间隔严格等于发送周期。此前观察到的 99.0%/99.8%「丢帧」实为拆设备时缓冲未冲干净。
3. **`usb_rx_frame_head_t.time_stamp` 的真实结构：高 32 位 = 秒，低 32 位 = 秒内纳秒**（低 32 位实测最大 998,940,532，从不 ≥1e9；粒度 4ns）。按平坦 uint64 解读会在每次秒进位产生 2^32−1e9 = 3.295s 的假跳变。
4. **模块时间戳时钟比主机慢 3.71%**（中位帧间隔/标称 = 0.9627~0.9631，跨 50 倍速率高度一致）。CAN 1Mbps 零错误说明晶振是准的 → 时间戳计数器用的是另一个时钟源（疑似内部 RC 振荡器，随温漂）。**修正系数 1.0385 不可硬编码，须现场标定。**
5. **推论：算速度时「主机发送周期」比 `hw_ts` 更准**（主机 steady_clock 晶振级 + 电机逐帧应答）。`hw_ts` 只用于批内排序与相对间隔。
6. **批内偶发乱序**：2/1759 = 0.11%（`hw_ts` 倒退 9.4ms / 25.1ms）。依赖顺序的代码必须按 `hw_ts` 排序。
7. 备选出路：仓库 `usb2canfd/firmware/socketcan/` 下有 gs_usb (`dm_usb2canfd_gsusb_1003.enc`) 与 slcan (`dm_usb2fdcan_slcan_1002.enc`) 固件，走 SocketCAN 可逐帧投递 + 内核时间戳。**刷固件需单独批准，且须先确认能刷回原厂。**

## 四、DM4310 电机的发现

### 寄存器实测（`0x33` 读回）

| 寄存器 | 值 |
|---|---|
| ESC_ID / MST_ID | 0x08 / 0x18 |
| CTRL_MODE | **1 = MIT 模式**（用户此前未说明，直接跑速度模式会不符预期） |
| can_br | 4 = 1Mbps |
| PMAX / VMAX / TMAX | 12.5 / 50 / 10（与旧 SDK `limit_param[DM4310_48V]` 硬编码一致，反馈解码直接套用） |
| NPP / Gr | 14 / 10 |
| TIMEOUT (0x09) | 400（单位手册未标） |
| 保护阈值 | 低压 15V / 过压 60V / 过温 100℃ / 过流 0.8 |

### 通讯丢失看门狗（踩坑三次换来的）
- 使能状态下总线静默过久 → `ERR=0xD`，电机**自动退出使能**。边界：120ms 必触发，~15ms 不触发。
- 故障位**锁存**，`0xFC` 不清错，须先发 `0xFB`。
- **不能「等 ack 确认使能再发帧」**——等待本身就是静默，就会触发。正确姿势：发完 `0xFC` **立即**进控制循环，宽限期后回读 ERR 校验（边发边校验）。

### 摩擦与运动特性（方案 B' 实测）
- **控制律闭环验证通过**：kd=0.3 巡航段实测 tau 0.080 Nm vs 理论 kd×(dq_des−dq)=0.0876 Nm，9% 内吻合。
- **kd 扫描**：首次挣脱齿槽在 kd≈0.46（≈0.129 Nm）；kd 到 0.76 才持续运动。低 kd 下为**粘滑运动**（stick-slip）：~0.0076 rad 的离散跳步夹着静止段。
- **kd=1.14 达成连续旋转**：3s 转过 31.5°，巡航速度 0.1727 rad/s = 指令的 61%，粘滑消失（仅 10% 采样 ≤0）。
- **摩擦不是常数——存在 Stribeck 效应**：两个工况点 0.164 Nm@0.065 rad/s vs 0.114 Nm@0.180 rad/s，斜率 ≈ −0.43 Nm/(rad/s)。负斜率即负阻尼，正是低速粘滑的教科书成因。
- **纯阻尼控制的结构性极限**：kp=0 无前馈时，稳态速度误差恒等于 T_friction/kd，不会消失；即使 kd 顶到 1.5 上限也只能到指令速度的 ~61%。瞬态可冲高（实测峰值 0.476 vs 指令 0.281 rad/s）——力矩会反号减速，但挡不住弹性能量释放瞬间的过冲。

### 反馈帧量化精度
位置 0.000381 rad/LSB；速度 0.0244 rad/s/LSB（中点码 2047 → −0.0122）；力矩 0.00488 Nm/LSB（0 Nm 编码为 2047）。

## 五、方法论教训（写下来防止再犯）

1. **链路延迟错了两次**：先把真实的「100ms」观察自我否定为代码假象；又用结构性错误的 `dm_probe --latency`（拿每帧到达时刻减「最近一次发送」——一批 10 帧同到时全都只和最后一次发送比，攒包在结构上不可见）测出 16~22ms 并写进文档。**正确做法：逐帧配对自己的发送时刻，或看到达间隔的分布（双峰一眼可见），均值必然掩盖。**
2. `hw_ts` 平坦解码制造的 3.295s 假跳变，一度被当成「模块异常卡顿」。
3. 「静摩擦 0.2133 Nm」高估 ~65%——门限 0.02 rad 需累积 3~4 个齿槽才触发，是我自己设计的门限引入的偏差。修为双门限报告。
4. `dm_spin.cpp` 里「算速度必须用 hw_ts 做时基」的注释是**反的**（尚未改），主机发送周期才是更准的时基。

## 六、当前状态与待决事项

- 四个例程均编译通过并由用户实际运行验证；数据文件：`dmcan-ws/build/run_kd114.csv`（398 行，无 hw_ts 列）、`dmcan-ws/build/batch.csv`（1760 行，含 hw_ts）。
- 待办：
  1. 改掉 `dm_spin.cpp` 里那条反了的 hw_ts 注释；
  2. 更新 `PROGRESS-2026-08-02.md` §4.5（仍留着作废的 16~22ms 数字），补入攒包/hw_ts/Stribeck 结论；
  3. **用户决策点**：接受 10Hz 反馈上限继续封装协议层，还是走 SocketCAN 固件路线（需单独批准 + 确认可回刷原厂）。

## 安全约束（持续有效）

> 电机未固定，直接摆在桌面上，只能低转速驱动。**任何使能/启动前必须先告知用户并获批准。**

已获批范围：`0xFB` 清错、方案 B' 运动测试（用户人工握持外壳 + 急停待命）。**从未发送/从未批准**：`0x55` 写参数、`0xAA` 存参数、`0xFE` 存零点、固件刷写。
